import hello from './hello'
import schema from './schema'
import server from './server'
import build from './build'
import deploy from './deploy'

export default { hello, schema, server, build, deploy }